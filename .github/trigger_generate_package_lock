This file was added as a temporary no-op trigger to start the Generate package-lock workflow.

The generate-package-lock workflow has been reverted to manual dispatch only (workflow_dispatch), so this file is no longer needed and is safe to delete.

I cannot delete files directly from the repository with the current automation, so please delete this file manually if you want it removed entirely.

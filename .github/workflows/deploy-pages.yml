name: Old deploy-pages (disabled)

# This workflow used to auto-deploy on push but was causing failures (deprecated actions).
# It's been disabled by setting it to manual dispatch only. Delete this file to remove it completely.

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "This workflow has been disabled. Delete .github/workflows/deploy-pages.yml to remove it permanently."
